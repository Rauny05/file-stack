# Absolute path to the folder containing your portfolio project directories
PORTFOLIO_ROOT=/path/to/your/portfolio/folder

# Port (optional, defaults to 3000)
PORT=3000

# Comma-separated list of allowed frontend origins (optional, defaults to localhost)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
